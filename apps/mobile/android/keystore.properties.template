# Copy this file to keystore.properties (same directory) and fill in your values.
# keystore.properties is gitignored — do NOT commit it or your keystore.
# storeFile is resolved relative to the android/ directory (this file's dir),
# or you may use an absolute path.
storeFile=app/upload-keystore.jks
storePassword=CHANGE_ME
keyAlias=upload
keyPassword=CHANGE_ME
