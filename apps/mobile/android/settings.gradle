pluginManagement { includeBuild("../node_modules/@react-native/gradle-plugin") }
plugins { id("com.facebook.react.settings") }
extensions.configure(com.facebook.react.ReactSettingsExtension){ ex -> ex.autolinkLibrariesFromCommand() }

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        maven { url = uri("https://jitpack.io") }
        // @notifee/react-native ships its app.notifee:core AAR as a local
        // maven repo inside the package (PREFER_SETTINGS ignores repos
        // declared at the project level, so it must live here).
        maven { url = uri("$rootDir/../node_modules/@notifee/react-native/android/libs") }
    }
}

rootProject.name = 'Stavi'
include ':app'
includeBuild('../node_modules/@react-native/gradle-plugin')
