// Barrel for terminal plugin's component directory.
// Phase C2.1 adds SkiaTerminalView as a standalone component — not yet
// wired into the plugin (Phase C3 will handle that switcher).

export { SkiaTerminalView } from './SkiaTerminalView';
export { default as SkiaTerminalViewDefault } from './SkiaTerminalView';
